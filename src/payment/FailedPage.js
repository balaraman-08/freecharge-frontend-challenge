import { Typography } from "@material-ui/core";
import React from "react";

const FailedPage = () => {
  return (
    <div style={{ textAlign: "center", color: "red", marginTop: "2rem" }}>
      <Typography variant="h2">Failure</Typography>
    </div>
  );
};

export default FailedPage;
